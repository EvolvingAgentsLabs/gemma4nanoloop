# Copy to .env (gitignored) and fill in. Never commit the real file.
# Used only by the `aistudio` backend, which is a measurement oracle for
# eval --oracle, not the development runtime. See IMPLEMENTATION.md.
#
# This is AI Studio's credential name, not a model choice: the backend it
# unlocks serves gemma-4-26b-a4b-it. Gemma 4 is the only family this harness
# runs and model_ollama.check_model() enforces it.
GEMINI_API_KEY=
